{
  "name": "my-daily-tasks",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "test": "node --test tests/domain.test.js tests/app.test.js",
    "check": "node scripts/check.mjs",
    "build": "node scripts/build.mjs",
    "test:rules": "firebase emulators:exec --project demo-my-daily-tasks --only firestore \"node --test tests/rules.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "3.0.4",
    "firebase": "10.8.1",
    "firebase-tools": "13.35.1",
    "jsdom": "26.1.0"
  }
}
